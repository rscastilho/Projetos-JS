function verificaNumero(...lista){
    let itens = [];
    for (let i=0; i < lista.length; i++){
        if(typeof lista[i] !== 'number'){
            console.log('valor', lista[i],'na posicao', [i], "nao é numero")
        }
            let numeros =[];
            numeros.push(lista[i]);
        
            
        
        
    }
}

verificaNumero('a',2,3,'b',5)